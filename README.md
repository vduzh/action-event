# Building and Publishing

To build the library:
```bash
./gradlew build
```

To test the library:
```bash
./gradlew test
```

To publish the artifact to the repository:
```bash
./gradlew publish
```

